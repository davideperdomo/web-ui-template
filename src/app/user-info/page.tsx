import UserView from "@ui/user/components/UserViewBloc";

/**
 * Using UserView with BLoC pattern.
 * Hooks implementation available con /UserViewHook
 */
const UserPage = () => <UserView />;

export default UserPage;
